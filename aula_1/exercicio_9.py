readFaren = float(input('Digite a temperatura em fahrenheint para a conversao: '))
convertCelsius = 5 * ((readFaren-32) / 9)

print('Convertido para celsius, a temperatura fica: %.1f', convertCelsius)